version: "3.9"
   
services:
  web:
    build: .
    env_file:
      - ./.env
    tty: true
    command: python3 ./manage.py runserver 0:8001
    restart: always
    volumes:
      - .:/code
    ports:
      - "8001:8001"
networks:
  frontend:
    driver: nginxproxymanager_default
